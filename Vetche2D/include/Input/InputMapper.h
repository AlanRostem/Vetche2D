#pragma once
#include "../Core/Engine.h"

namespace Vetche2D {
	class InputMapper {
	public:
		//Used to check for user input and map a certain call to that input
		InputMapper();
		~InputMapper();
	};
}